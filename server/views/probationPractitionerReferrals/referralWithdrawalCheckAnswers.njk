{% from "govuk/components/button/macro.njk" import govukButton %}
{% from "govuk/components/back-link/macro.njk" import govukBackLink %}
{% from "govuk/components/radios/macro.njk" import govukRadios %}

{% extends "../partials/layout.njk" %}

{% set pageTitle = "Referral withdrawal" %}
{% set pageSubTitle = "Confirm" %}

{% block pageContent %}
  <div class="govuk-grid-row">
    <div class="govuk-grid-column-full">

    {{ govukBackLink(backLinkArgs) }}

      <h1 class="govuk-heading-xl">{{ presenter.text.title }}</h1>
      <p class="govuk-body-m govuk-!-margin-bottom-8">{{ presenter.text.confirmationQuestion }}</p>
      <form method="post" action="{{ presenter.confirmWithdrawalHref }}">
        <input type="hidden" name="_csrf" value="{{csrfToken}}">

        {{ govukRadios(confirmWithdrawalButtonArgs) }}

        {{ govukButton({ text: "Confirm and send", preventDoubleClick: true }) }}
      </form>
    </div>
  </div>
{% endblock %}
